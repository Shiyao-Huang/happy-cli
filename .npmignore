# Exclude unpacked binaries from npm package
tools/unpacked/

# Keep these for npm package
# tools/archives/
# tools/licenses/

# Environment variable files (never ship in npm package)
.env
.env.*
!.env.example